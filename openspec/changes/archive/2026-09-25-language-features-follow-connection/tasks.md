## 1. Server

- [x] 1.1 `initialize` sends the capabilities at the top level; test
- [x] 1.2 `oxilite/documentConnection`; completion, hover and vocabulary warnings use the
      document's connection; Cypher options use the queried connection's vocabulary; test

## 2. Extension

- [x] 2.1 `DocumentConnections` sends pinned files' and notebook cells' connections
- [x] 2.2 Background attach only when nothing is created or asked; concurrent attaches merged
- [x] 2.3 Verified in VS Code: completion, hover and diagnostics work; a pinned file completes
      from its database

## 3. Docs and release

- [x] 3.1 `lat.md` in both repositories; `lat check`
- [x] 3.2 Version 0.1.3, packages, publish
